# Copy this file to .env for local Docker Compose usage.
# Replace these values with local-only credentials.

DB_NAME=orderdb
DB_USER=replace_with_local_username
DB_PASSWORD=replace_with_local_password
